# AssetFlow Environment Settings Configuration Template

# CORS settings: comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8080


# Secret Key (long secure string for production JWT token signatures)
SECRET_KEY=dev-secret-key-change-in-production-must-be-long-and-secure
